# Contributing

1. Fork the repo
2. Create a feature branch (`git checkout -b feature/my-change`)
3. Make your changes
4. Open a PR against `main`

This is a vanilla JS project with no build step — no need to install dependencies for development.
